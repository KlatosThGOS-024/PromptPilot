{
  "name": "promptpilot",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install-all": "npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm run dev"
  }
}
